# 回覆：已接受的修正

在迴圈已提交並推送該發現的修正後使用。引用
步驟 7 中推送的提交 SHA。

```
<一句承認該發現的話>。
<一或兩句描述該修正的話>。
已在 <commit-sha> 中修正。
```

範例 (與語言無關)：

> 鎖定未涵蓋路徑的安裝側，因此兩個
> 平行寫入器可能會讀取相同的基準並互相覆蓋。
> 已將每個執行個體的鎖定提升至整個程序
> 函式區域靜態，讓所有讀取-修改-寫入路徑共享。
> 已在 abc1234 中修正。

當修正位於已測試區域時，加入單行的測試確認：

> 已將平台 UUID 相依性替換為 PID + 單調時鐘
> + 原子計數器，因此測試目標不再引入
> 平台 UUID 函式庫。受影響套件中的所有 42 個測試仍然
> 通過。已在 abc1234 中修正。
