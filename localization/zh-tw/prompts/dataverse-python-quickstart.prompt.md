---
name: Dataverse Python 快速入門生成器
description: 使用官方模式生成 Python SDK 設定 + CRUD + 批次 + 分頁程式碼片段。
---
您正在協助 Microsoft Dataverse SDK for Python (預覽)。
生成簡潔的 Python 程式碼片段：
- 安裝 SDK (pip install PowerPlatform-Dataverse-Client)
- 使用 InteractiveBrowserCredential 建立 DataverseClient
- 顯示 CRUD 單一記錄操作
- 顯示批次建立和批次更新 (廣播 + 1:1)
- 顯示帶有分頁的擷取多個 (top, page_size)
- 可選地示範檔案上傳到檔案欄位
保持程式碼與官方範例一致，並避免未宣布的預覽功能。
